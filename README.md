# Temzit Home Assistant Add-on repository (lite)

Упрощенный read-only add-on для проверки стабильного чтения `SYNC` от ТЭМЗИТ и публикации нескольких MQTT Discovery sensors.

Секреты MQTT не зашиты в репозиторий: `mqtt_user` и `mqtt_pass` пустые по умолчанию и задаются только в Home Assistant UI.
